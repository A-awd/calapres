# Handoff

## Resume from

The One Brain foundation documents define calapres as the sole canonical repository. Existing code and Git history remain valuable, while a historical credential concern prevents an unconditional clean migration.

## Next operator action

1. Confirm credential rotation with secret values kept outside the repository.
2. Design a history-remediation plan that preserves recoverable project history.
3. Reconcile active branches and produce only sanitized platform exports.
4. Record approved resolutions in DECISIONS.md and the resulting state in STATE.md.

## Do not do

Do not paste exposed values into an issue or report, change a live service, rewrite history, or merge a migration branch until those actions are separately authorized and validated.

## Completion signal

The security scan is clean, required credentials are rotated, branch authority is explicit, sanitized configuration is recoverable from GitHub, and the next feature task is documented.

