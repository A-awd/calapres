# Recovered local theme variant

This directory preserves one unique Shopify home-page template variant recovered
from local stash evidence during the One Brain Phase 2 cleanup review.

The active `templates/index.json` is unchanged. Treat this copy as review-only:
compare its section order with the current theme and do not merge it into the
active template until the storefront owner confirms the intended layout.
