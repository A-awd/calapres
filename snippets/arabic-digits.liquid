{%- comment -%}
  Legacy compatibility snippet. Shopify numeric values already use
  Western 0-9 digits, so keep them unchanged on every storefront locale.
  Usage: {% render 'arabic-digits', value: cart.item_count %}
{%- endcomment -%}
{{- value | append: '' -}}
